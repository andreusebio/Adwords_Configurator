import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme) => ({
  configuratorBox: {
    backgroundColor: "#15507d",
    display: "flex",
    padding: "2rem",
    flexWrap: "wrap",
  },
}));
