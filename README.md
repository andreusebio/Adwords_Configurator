# Adwords_Configurator
Demo React module with with multiple custom made form fields. 
Includes Node.js Backend and Mysql database
Ready to run on docker

# How to run with docker
- docker-compose build
- docker-compose up
